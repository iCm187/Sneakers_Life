<!DOCTYPE html>
<lang="en" data-bs-theme="light">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<link rel="icon" type="image/png" href="{{ asset('') }}"/>
		<title>
			{% block title %}Sneakers Life
			{% endblock %}
		</title>
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}
		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}

	</head>
	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{ path('app_home') }}">HOME</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
				<div class="collapse navbar-collapse" id="navbarSupportedContent">
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								Les actualités sneakers
							</a>
							<ul class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown">
								<li class="dropdown-menu bg-dark" aria-labelledby="navbarDropdown"></li>
								<li>
									<a class="dropdown-item text-white" href="{{ path('app_article_index') }}">Les nouveautés</a>
								</li>
							</ul>
						</li>
						<li>
							<a class="dropdown-item text-white py-2" href="{{ path('app_admin') }}">Administration</a>
						</li>
						{% if app.user %}
						<li>
							<a class="dropdown-item nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
						</li>
						{% endif %}
					</ul>
				</div>
			</div>
	</nav>
	<body>
		{% block body %}{% endblock %}
	</body>
</html>
