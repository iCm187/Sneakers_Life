{% extends 'base.html.twig' %}

{% block title %}Édition mon article {% endblock %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}
	<div class="bg-black" style="min-height: 90vh; background-color:black;">

		<div class='container w-50 text-white pt-5'>
			<div class="mb-5">
				<h1 class="d-flex justify-content-center">Éditer l'article</h1>
			</div>

			{{ include('article/_form.html.twig', {'button_label': 'Update'}) }}
			<div class="mt-4">
				<a href="{{ path('app_article_index') }}">back to list</a>
			</div>
			{{ include('article/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
