{% extends 'base.html.twig' %}

{% block body %}

    <div class="bg-black" style="min-height: 100vh; background-color: black;">
        <div>

            <h1 class="text-center text-white pt-5">{{ sneakers.name }}</h1>
            <div class="row row-cols-1 row-cols-md-4 g-4 px-3 m-4">
                <img src="{{ sneakers.images }}">

                    <div class="col">
                    <p class="text-white ms-5">{{ sneakers.gender }}</p>
                    </div>
                    <div class="col">
                    <p class="text-white ms-5">{{ sneakers.style }}</p>
                    </div>
            </div>
        </div>
    </div>
{% endblock %}